package ruckleKnuckle;

import java.awt.event.ActionEvent;
import java.awt.event.MouseEvent;

import javax.swing.JButton;

import menus.MainMenu;
import acm.program.*;
import acm.graphics.*;

public class Main extends GraphicsProgram {
/*
	private static final int WINDOW_WIDTH = 1280;
	private static final int WINDOW_LENGTH = 720;
	private static State state = State.MAIN_MENU;

	public void run() {
		resize(WINDOW_WIDTH, WINDOW_LENGTH);
		addMouseListeners();
		addKeyListeners();
		addActionListeners();
		

		goTo(State.MAIN_MENU);

	}

	public void keyPressed() {

	}

	public void mousePressed(MouseEvent e) {
		switch (state) {
		case MAIN_MENU:
			mousePressedMainMenu(e);
			break;
		case CHAR_SELECTION:
			break;
		case CONTROLS:
			break;
		case CREDITS:
			break;
		case GAME_OVER:
			break;
		default:
			break;
		}
	}

	private void goTo(State s) {
		removeAll();
		switch (s) {
		case MAIN_MENU:
			for (GObject o : MainMenu.getInstance().getButtons()) {
				add(o);
			}
			break;
		case CHAR_SELECTION:
			break;
		case CONTROLS:
			break;
		case CREDITS:
			break;
		case GAME_OVER:
			break;
		default:
			break;
		}

	}

	private void mousePressedMainMenu(MouseEvent e) {
		
	}

	public void actionPerformed(ActionEvent e) {
		switch (e.getActionCommand()) {
		case "CHAR_SELECTION":
			goTo(State.CHAR_SELECTION);
			break;
		case "CONTROLS":
			goTo(State.CONTROLS);
			break;
		case "CREDITS":
			goTo(State.CREDITS);
			break;
		}

	}*/

}
